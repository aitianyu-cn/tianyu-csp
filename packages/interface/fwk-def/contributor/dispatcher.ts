/** @format */

/**
 * Dispatch handler job running type
 *
 * @example
 * network  // indicates it is a network request
 * job      // indicates it is a schedule job
 */
export type DispatcherTriggerType = "network" | "job";
